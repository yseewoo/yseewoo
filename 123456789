/*size(480,120);
strokeWeight(8);
for(int i=20;i<400;i=i+60){
  line(i,40,i+60,80);
}
background(0);
noStroke();
for(int y=0 ; y<=height; y+=40){
  for(int x=0; x<=width; x+=40){
    fill(255,140);
    ellipse(x,y,40,40);
  }}*/
