int x=120;
int y=60;
int w=80;
int h=60;

void setup(){
  size(860,600);
 }

void draw(){
  background(255,0,0);
  if((mouseX>x)&&(mouseX<x+w)&&(mouseY>y)&&(mouseY<y+h)){
    fill(0);
  }
  else{
    fill(255);
  }
  rect(x,y,w,h);

 }
